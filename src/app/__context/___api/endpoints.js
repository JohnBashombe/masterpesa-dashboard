const API_VESRION = '/api';

const __ENDPOINTS = Object.freeze({
  LOGIN_EMAIL_URL: `${API_VESRION}/admin/email/login`,
  LOGIN_PASSWORD_URL: `${API_VESRION}/admin/password/login`,
});

export default __ENDPOINTS;
